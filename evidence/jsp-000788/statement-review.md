# JSP-000788 / Erdős 948: statement and attribution review

The [original question](https://www.erdosproblems.com/948) asks for a rate f and a finite palette such that every coloring admits a strictly increasing sequence with a_n < f(n) infinitely often whose finite-sums set omits a color. The source constructs, for every f and every nonempty finite palette, a coloring under which every such sequence hits every color. Its terminal `Erdos948.not_erdos_948` negates the original quantifier pattern, with integer sequence values and the infinite-often condition unchanged.

The finite-sum predicate in the terminal includes the empty set, as does the displayed finite-set wording of the cached original question. The preceding `finite` theorem actually supplies a nonempty finite subset for each desired color, so the negative answer is not an artifact of coloring the empty sum. `finite_nat` also records the natural-valued sequence version. This packet does not claim other growth-rate refinements beyond these statements.

**Unresolved name discrepancy, explicitly preserved:** the unchanged proof header lists “Lisa Price” and GPT-5.5 Pro as informal authors, while the [primary forum thread](https://www.erdosproblems.com/forum/thread/948) contains a proof announcement signed **Liam Price**, dated 21 June 2026, 20:38, and the prize catalog also names Liam Price. The thread describes that contributor's Aristotle formalization; the selected plby file separately credits Codex and GPT-5.6 Sol as formal authors. These are not silently conflated. The spelling difference may be a header error, but this review does not assert that Lisa and Liam are the same person, resolve actual authorship by inference, or nominate either as an award recipient. Both source records are supplied for curatorial attribution review. The checking account claims no original proof authorship.

The exact source SHA-256 is `7e1c21956a12643c28ade9d2a246a57cf0f8159b0342bcfa682dee913624015f`. Its compilation, the terminal theorem/axiom audit and `leanchecker --verbose Erdos948` all exited 0. The terminal axioms are propext, Classical.choice, Quot.sound. These checks establish the observed proof result in the pinned environment and do not resolve names, prize eligibility or recipient confirmation. The checker uses Lean's own kernel and trusts imported Mathlib caches; it is not an independent implementation or a fresh full dependency replay.
