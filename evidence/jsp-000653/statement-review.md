# Erdős 795: selected statement review

[Original question](https://www.erdosproblems.com/795).

The extremal g(N) ranges over subsets of [1,N] whose product map is injective on the full powerset, including the empty subset with product one. The selected terminal upper bound is π(N)+π(floor sqrt N)+o(sqrt N/log N), expressed by a uniform eventual inequality for every ε>0. The development also supplies the prime/prime-square baseline lower construction. This closes the selected asymptotic question, not every additional exact-formula conjecture mentioned in historical commentary.

The root review checks these definitions and terminal quantifiers against the prior source audit and pinned proof. All original proof bytes, author headers and license notices are preserved. This is existing-proof verification, not a claim of a new mathematical result or new proof authorship.

Successful compilation and target-module replay must be established by the actual `verified-run/verification.json` and logs; this preparatory review alone is not a kernel check. The checker uses the same Lean kernel and trusts pinned imported Mathlib caches.
